import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  image: {
    height: 200,
    width: 200
  },
  container: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center'
  }
})
